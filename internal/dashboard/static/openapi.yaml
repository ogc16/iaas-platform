openapi: 3.0.3
info:
  title: IaaS Platform API
  description: |
    A Go-based cloud control plane and billing platform. Multi-tenant
    organizations, compute resource lifecycle management, API key
    authentication, rate limiting, quotas, usage-based billing, signed
    webhook notifications, an append-only audit trail, and Prometheus-format
    metrics. The compute backend is a simulation; the control-plane API is real.
  version: 1.0.0
servers:
  - url: http://localhost:8080/api/v1
    description: Control-plane API
  - url: http://localhost:8080
    description: Health probes (served on the server root)
tags:
  - name: auth
  - name: organizations
  - name: compute
  - name: billing
  - name: webhooks
  - name: audit
  - name: metrics
  - name: health
paths:
  /healthz:
    get:
      tags: [health]
      summary: Liveness probe
      description: Always returns 200 while the process is up. Never depends on external state.
      responses:
        '200':
          description: Process is alive
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
  /readyz:
    get:
      tags: [health]
      summary: Readiness probe
      description: Returns 200 when the database is reachable, 503 otherwise.
      responses:
        '200':
          description: Server can serve traffic
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
        '503':
          description: Database unreachable
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: unavailable
                  reason:
                    type: string
                    example: database unreachable
  /auth/signup:
    post:
      tags: [auth]
      summary: Create an account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignupRequest'
      responses:
        '201':
          description: Account created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Email already taken
  /auth/login:
    post:
      tags: [auth]
      summary: Log in
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          description: Invalid credentials
  /auth/forgot-password:
    post:
      tags: [auth]
      summary: Request a password reset email
      description: Always reports success for valid requests so callers cannot enumerate which emails have accounts.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ForgotPasswordRequest'
      responses:
        '200':
          description: Reset email sent (or would have been sent)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForgotPasswordResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
  /auth/reset-password:
    post:
      tags: [auth]
      summary: Reset a password with a one-time token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordRequest'
      responses:
        '200':
          description: Password updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ForgotPasswordResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
  /me:
    get:
      tags: [auth]
      summary: Get the current user
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: The authenticated user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /orgs:
    post:
      tags: [organizations]
      summary: Create an organization
      security:
        - bearerAuth: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrgRequest'
      responses:
        '201':
          description: Organization created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          description: Slug already taken
    get:
      tags: [organizations]
      summary: List organizations for the current user
      security:
        - bearerAuth: []
        - apiKey: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Organizations
          headers:
            X-Total-Count:
              $ref: '#/components/headers/XTotalCount'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Organization'
  /orgs/{orgID}:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    get:
      tags: [organizations]
      summary: Get an organization
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Organization
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/members:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    post:
      tags: [organizations]
      summary: Invite a member
      security:
        - bearerAuth: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InviteMemberRequest'
      responses:
        '201':
          description: Member added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgMember'
        '400':
          $ref: '#/components/responses/BadRequest'
    get:
      tags: [organizations]
      summary: List organization members
      description: |
        Admins see every member. Regular members only ever see their own
        membership, so they cannot enumerate other members or their details.
      security:
        - bearerAuth: []
        - apiKey: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Members
          headers:
            X-Total-Count:
              $ref: '#/components/headers/XTotalCount'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrgMember'
  /orgs/{orgID}/members/{userID}:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/UserID'
    delete:
      tags: [organizations]
      summary: Revoke a member's access
      description: Removes a user from the organization and clears any pending join request. Admins only.
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Access revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: revoked
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/members/{userID}/suspend:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/UserID'
    post:
      tags: [organizations]
      summary: Suspend a member temporarily
      description: >-
        Revokes a member's access for the given number of days (1-365). Rights
        are restored automatically once the period elapses; an admin can also
        restore access early via unsuspend. Admins only.
      security:
        - bearerAuth: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuspendMemberRequest'
      responses:
        '200':
          description: Member suspended
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgMember'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/members/{userID}/unsuspend:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/UserID'
    post:
      tags: [organizations]
      summary: Unsuspend a member
      description: Immediately restores a suspended member's access. Admins only.
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Member unsuspended
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgMember'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/requests:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    get:
      tags: [organizations]
      summary: List pending join requests
      description: Users who signed up with this organization's slug and are waiting for approval. Admins only.
      security:
        - bearerAuth: []
        - apiKey: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Join requests
          headers:
            X-Total-Count:
              $ref: '#/components/headers/XTotalCount'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/JoinRequest'
        '400':
          $ref: '#/components/responses/BadRequest'
  /orgs/{orgID}/requests/{userID}/accept:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/UserID'
    post:
      tags: [organizations]
      summary: Accept a join request
      description: Adds the user as a member of the organization and removes the pending request. Admins only.
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Member added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrgMember'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: User is already a member
  /orgs/{orgID}/requests/{userID}/revoke:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/UserID'
    post:
      tags: [organizations]
      summary: Revoke a join request
      description: Denies a pending join request. Admins only.
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Request revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: revoked
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/instances:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    post:
      tags: [compute]
      summary: Create an instance
      security:
        - bearerAuth: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateInstanceRequest'
      responses:
        '201':
          description: Instance created (pending)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComputeInstance'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          description: Not a member of this organization
        '409':
          description: Quota exceeded or insufficient regional capacity
    get:
      tags: [compute]
      summary: List instances in an organization
      security:
        - bearerAuth: []
        - apiKey: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Instances
          headers:
            X-Total-Count:
              $ref: '#/components/headers/XTotalCount'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ComputeInstance'
  /orgs/{orgID}/instances/{instanceID}:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/InstanceID'
    get:
      tags: [compute]
      summary: Get an instance
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Instance
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComputeInstance'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/instances/{instanceID}/start:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/InstanceID'
    post:
      tags: [compute]
      summary: Start an instance (async)
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '202':
          description: Transition accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransitionAccepted'
        '409':
          description: Invalid state transition
  /orgs/{orgID}/instances/{instanceID}/stop:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/InstanceID'
    post:
      tags: [compute]
      summary: Stop an instance (async)
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '202':
          description: Transition accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransitionAccepted'
        '409':
          description: Invalid state transition
  /orgs/{orgID}/instances/{instanceID}/terminate:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/InstanceID'
    post:
      tags: [compute]
      summary: Terminate an instance (async)
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '202':
          description: Transition accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransitionAccepted'
        '409':
          description: Invalid state transition
  /orgs/{orgID}/billing/usage:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    get:
      tags: [billing]
      summary: Get usage summary for the last 30 days
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Usage summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageSummary'
    post:
      tags: [billing]
      summary: Record usage
      security:
        - bearerAuth: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordUsageRequest'
      responses:
        '201':
          description: Usage recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: recorded
        '400':
          $ref: '#/components/responses/BadRequest'
  /orgs/{orgID}/billing/invoices:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    get:
      tags: [billing]
      summary: List invoices
      security:
        - bearerAuth: []
        - apiKey: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Invoices
          headers:
            X-Total-Count:
              $ref: '#/components/headers/XTotalCount'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Invoice'
  /orgs/{orgID}/billing/invoices/generate:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    post:
      tags: [billing]
      summary: Generate an invoice for the current month
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '201':
          description: Invoice generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invoice'
  /orgs/{orgID}/billing/invoices/{invoiceID}:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/InvoiceID'
    get:
      tags: [billing]
      summary: Get invoice line items
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: Invoice line items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/InvoiceLineItem'
  /orgs/{orgID}/webhooks:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    get:
      tags: [webhooks]
      summary: List organization webhooks
      description: Admins see all webhooks; other members receive 403.
      security:
        - bearerAuth: []
        - apiKey: []
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: List of webhooks
          headers:
            X-Total-Count:
              $ref: '#/components/headers/XTotalCount'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Webhook'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [webhooks]
      summary: Register a webhook
      description: |
        Registers an outbound endpoint subscribed to one or more events.
        When no secret is supplied the platform generates one and returns it
        once in the created webhook object.
      security:
        - bearerAuth: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWebhookRequest'
      responses:
        '201':
          description: Webhook created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Webhook'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
  /orgs/{orgID}/webhooks/{webhookID}:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/WebhookID'
    get:
      tags: [webhooks]
      summary: Get a webhook
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '200':
          description: The webhook
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Webhook'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [webhooks]
      summary: Update a webhook
      description: Only the fields supplied are changed; nil fields are left untouched.
      security:
        - bearerAuth: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWebhookRequest'
      responses:
        '200':
          description: The updated webhook
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Webhook'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [webhooks]
      summary: Delete a webhook
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '204':
          description: Webhook deleted
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/webhooks/{webhookID}/ping:
    parameters:
      - $ref: '#/components/parameters/OrgID'
      - $ref: '#/components/parameters/WebhookID'
    post:
      tags: [webhooks]
      summary: Test a webhook
      description: Queues a synthetic `ping` delivery to the endpoint so the owner can verify connectivity.
      security:
        - bearerAuth: []
        - apiKey: []
      responses:
        '202':
          description: Test delivery queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: accepted
                  action:
                    type: string
                    example: ping
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /orgs/{orgID}/audit:
    parameters:
      - $ref: '#/components/parameters/OrgID'
    get:
      tags: [audit]
      summary: List the organization's audit trail
      description: |
        Admins see every recorded action; regular members are scoped to their
        own actions. Results are ordered newest first.
      security:
        - bearerAuth: []
        - apiKey: []
      parameters:
        - name: action
          in: query
          required: false
          description: Only return events matching this action (e.g. `instance.create`).
          schema:
            type: string
        - name: resource
          in: query
          required: false
          description: Only return events for this resource (e.g. `instance`).
          schema:
            type: string
        - name: user_id
          in: query
          required: false
          description: Only return events performed by this user.
          schema:
            type: integer
            format: int64
        - name: since
          in: query
          required: false
          description: Only return events on or after this RFC3339 timestamp.
          schema:
            type: string
            format: date-time
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Audit events
          headers:
            X-Total-Count:
              $ref: '#/components/headers/XTotalCount'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AuditEvent'
        '403':
          $ref: '#/components/responses/Forbidden'
  /metrics:
    get:
      tags: [metrics]
      summary: Prometheus-format metrics
      description: |
        Metrics endpoint. When METRICS_TOKEN is configured, requests must carry
        it as a bearer token in the Authorization header.
      security:
        - metricsAuth: []
      responses:
        '200':
          description: Prometheus text-format metrics
          content:
            text/plain:
              schema:
                type: string
        '401':
          description: Missing or invalid metrics token
components:
  headers:
    XTotalCount:
      description: Total number of items available, not just the current page
      schema:
        type: integer
        format: int64
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    apiKey:
      type: apiKey
      in: header
      name: X-API-Key
    metricsAuth:
      type: http
      scheme: bearer
      description: Bearer token set via METRICS_TOKEN. Omitted when no token is configured.
  parameters:
    Limit:
      name: limit
      in: query
      required: false
      description: Maximum number of items to return (default 50, max 100)
      schema:
        type: integer
        minimum: 1
        maximum: 100
    Offset:
      name: offset
      in: query
      required: false
      description: Number of items to skip before returning results
      schema:
        type: integer
        minimum: 0
    OrgID:
      name: orgID
      in: path
      required: true
      schema:
        type: integer
        format: int64
    UserID:
      name: userID
      in: path
      required: true
      schema:
        type: integer
        format: int64
    InstanceID:
      name: instanceID
      in: path
      required: true
      schema:
        type: integer
        format: int64
    InvoiceID:
      name: invoiceID
      in: path
      required: true
      schema:
        type: integer
        format: int64
    WebhookID:
      name: webhookID
      in: path
      required: true
      schema:
        type: integer
        format: int64
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthorized:
      description: Missing or invalid credentials
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Forbidden:
      description: The caller does not have permission for this action
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: string
          example: invalid request
    SignupRequest:
      type: object
      required: [email, password, name]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 8
        name:
          type: string
        organization:
          type: string
        org_slug:
          type: string
          description: Slug of an existing organization to request access to. The organization admin must accept before access is granted.
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    AuthResponse:
      type: object
      properties:
        token:
          type: string
        api_key:
          type: string
          description: Raw API key. Returned only once, on signup; the server stores a SHA-256 hash.
        org_join_pending:
          type: boolean
          description: True when the account requested access to an organization and is awaiting admin approval.
        user:
          $ref: '#/components/schemas/User'
    ForgotPasswordRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
    ResetPasswordRequest:
      type: object
      required: [token, new_password]
      properties:
        token:
          type: string
          description: Single-use reset token from the password reset email.
          minLength: 20
        new_password:
          type: string
          format: password
          minLength: 8
    ForgotPasswordResponse:
      type: object
      properties:
        status:
          type: string
          example: sent
    User:
      type: object
      properties:
        id:
          type: integer
          format: int64
        email:
          type: string
        name:
          type: string
        role:
          type: string
        organization:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateOrgRequest:
      type: object
      required: [name, slug]
      properties:
        name:
          type: string
          minLength: 3
          maxLength: 64
        slug:
          type: string
          minLength: 3
          maxLength: 32
    Organization:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        slug:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    InviteMemberRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
        role:
          type: string
          enum: [admin, member]
    SuspendMemberRequest:
      type: object
      required: [days]
      properties:
        days:
          type: integer
          minimum: 1
          maximum: 365
          description: Number of days the member is suspended for; access is restored automatically afterwards.
    OrgMember:
      type: object
      properties:
        id:
          type: integer
          format: int64
        organization_id:
          type: integer
          format: int64
        user_id:
          type: integer
          format: int64
        name:
          type: string
          description: Display name of the member's user account.
        email:
          type: string
          format: email
        role:
          type: string
        suspended_until:
          type: string
          format: date-time
          description: When present, the member's access is revoked until this time, after which it is restored automatically.
        created_at:
          type: string
          format: date-time
    JoinRequest:
      type: object
      properties:
        id:
          type: integer
          format: int64
        organization_id:
          type: integer
          format: int64
        user_id:
          type: integer
          format: int64
        email:
          type: string
          format: email
        name:
          type: string
        created_at:
          type: string
          format: date-time
    CreateInstanceRequest:
      type: object
      required: [name, instance_type, region, disk_gb]
      properties:
        name:
          type: string
          maxLength: 64
        instance_type:
          type: string
          enum: [vm, container]
        region:
          type: string
          enum: [us-east-1, us-west-1, eu-west-1]
        image:
          type: string
          example: debian-12
        port:
          type: integer
          minimum: 0
          maximum: 65535
        cpu_cores:
          type: integer
          minimum: 1
        memory_mb:
          type: integer
          minimum: 1
        disk_gb:
          type: integer
          minimum: 1
    ComputeInstance:
      type: object
      properties:
        id:
          type: integer
          format: int64
        organization_id:
          type: integer
          format: int64
        user_id:
          type: integer
          format: int64
        name:
          type: string
        instance_type:
          type: string
          enum: [vm, container]
        status:
          type: string
          enum: [pending, running, stopping, stopped, terminating, terminated, failed]
        region:
          type: string
        provider_id:
          type: string
        image:
          type: string
        port:
          type: integer
        cpu_cores:
          type: integer
        memory_mb:
          type: integer
        disk_gb:
          type: integer
        ip_address:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TransitionAccepted:
      type: object
      properties:
        status:
          type: string
          example: accepted
        action:
          type: string
          enum: [start, stop, terminate]
    RecordUsageRequest:
      type: object
      required: [instance_id, resource_type, quantity]
      properties:
        instance_id:
          type: integer
          format: int64
          minimum: 1
        resource_type:
          type: string
          enum: [cpu_hours, memory_gb_hours, disk_gb_hours]
        quantity:
          type: number
          exclusiveMinimum: 0
    UsageSummary:
      type: object
      properties:
        cpu_hours:
          type: number
        memory_gb_hours:
          type: number
        disk_gb_hours:
          type: number
    Invoice:
      type: object
      properties:
        id:
          type: integer
          format: int64
        organization_id:
          type: integer
          format: int64
        amount_cents:
          type: integer
          format: int64
        currency:
          type: string
        status:
          type: string
          enum: [pending, paid, overdue]
        period_start:
          type: string
          format: date-time
        period_end:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
    InvoiceLineItem:
      type: object
      properties:
        id:
          type: integer
          format: int64
        invoice_id:
          type: integer
          format: int64
        description:
          type: string
        resource_type:
          type: string
          enum: [cpu_hours, memory_gb_hours, disk_gb_hours]
        quantity:
          type: number
        unit_price_cents:
          type: integer
          format: int64
        amount_cents:
          type: integer
          format: int64
    Webhook:
      type: object
      properties:
        id:
          type: integer
          format: int64
        organization_id:
          type: integer
          format: int64
        url:
          type: string
          format: uri
        events:
          type: array
          items:
            type: string
            enum: [instance.created, instance.updated, instance.terminated, org.member_added, org.member_removed, org.member_suspended, org.member_unsuspended, org.join_request_accepted, org.join_request_revoked, billing.usage_recorded, billing.invoice_generated]
        active:
          type: boolean
          description: Whether the webhook is currently receiving deliveries.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    CreateWebhookRequest:
      type: object
      required: [url, events]
      properties:
        url:
          type: string
          format: uri
          maxLength: 512
        events:
          type: array
          minItems: 1
          maxItems: 20
          items:
            type: string
            enum: [instance.created, instance.updated, instance.terminated, org.member_added, org.member_removed, org.member_suspended, org.member_unsuspended, org.join_request_accepted, org.join_request_revoked, billing.usage_recorded, billing.invoice_generated]
        secret:
          type: string
          minLength: 16
          maxLength: 128
          description: Optional shared secret. When omitted the platform generates one.
    UpdateWebhookRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
          maxLength: 512
        events:
          type: array
          maxItems: 20
          items:
            type: string
            enum: [instance.created, instance.updated, instance.terminated, org.member_added, org.member_removed, org.member_suspended, org.member_unsuspended, org.join_request_accepted, org.join_request_revoked, billing.usage_recorded, billing.invoice_generated]
        active:
          type: boolean
    AuditEvent:
      type: object
      properties:
        id:
          type: integer
          format: int64
        organization_id:
          type: integer
          format: int64
        user_id:
          type: integer
          format: int64
        actor_email:
          type: string
          format: email
        action:
          type: string
          enum: [org.create, org.member.invite, org.member.remove, org.member.suspend, org.member.unsuspend, org.join_request.accept, org.join_request.revoke, instance.create, instance.start, instance.stop, instance.terminate, instance.status, billing.usage.record, billing.invoice.generate]
        resource_type:
          type: string
        resource_id:
          type: string
        metadata:
          type: object
          additionalProperties: true
        ip:
          type: string
        request_id:
          type: string
        created_at:
          type: string
          format: date-time
